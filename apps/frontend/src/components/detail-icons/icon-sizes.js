// icon-sizes.js
// The one size table for this icon set. Lives in its own module so that both
// DetailIcons.jsx and the hand-drawn LinkedInIcon.jsx size through the same
// three numbers — a mark that sits in a different box from its neighbours is
// the exact inconsistency this table exists to prevent.

export const SIZES = { sm: 16, md: 20, lg: 24 };
